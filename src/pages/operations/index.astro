---
import OperationsPanel from '@components/OperationsPanel.tsx';

const { t, lang: locale } = Astro.locals;
const staff = Astro.locals.user || { name: 'Guest', role: 'GUEST' };
const todaysTasks = [];
const villaStatus = [];

<div class="min-h-screen bg-stone-50">
  <OperationsPanel client:only staff={staff} todaysTasks={todaysTasks} villaStatus={villaStatus} t={t} lang={locale} />
</div>
