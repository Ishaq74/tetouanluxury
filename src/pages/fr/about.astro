---
import Base from '@layouts/Base.astro';
const { t, lang: locale } = Astro.locals;
---
<Base>
  <h1>{t('about.title')}</h1>
  <p>{t('about.intro')}</p>
  <!-- Ajoutez ici le contenu de la page à propos, prêt pour la traduction -->
</Base>
